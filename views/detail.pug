doctype html
html
    head
        title Chi tiết công việc
        // 1. Fonts & CSS
        link(href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet")
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet")
        link(href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet")

        // 2. CSS Tùy chỉnh (Giống create.pug)
        style.
            body {
                font-family: 'Poppins', sans-serif;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                min-height: 100vh;
                display: flex;
                align-items: center;
                justify-content: center;
                padding: 20px;
            }
            .glass-card {
                background: rgba(255, 255, 255, 0.95);
                border-radius: 24px;
                box-shadow: 0 10px 30px rgba(0,0,0,0.2);
                border: none;
                width: 100%;
                max-width: 600px; /* Rộng hơn chút để chứa nội dung */
            }
            .label-text {
                font-size: 0.85rem;
                text-transform: uppercase;
                letter-spacing: 1px;
                color: #888;
                font-weight: 600;
                margin-bottom: 5px;
            }
            .content-box {
                background: #f8f9fa;
                border-radius: 12px;
                padding: 15px;
                border: 1px solid #eee;
            }
            .badge-custom {
                padding: 8px 16px;
                border-radius: 30px;
                font-weight: 500;
            }

    body
        div.container.d-flex.justify-content-center
            div.glass-card.p-4.p-md-5
                
                // 1. Tiêu đề + Nút đóng
                div.d-flex.justify-content-between.align-items-start.mb-4
                    h3.fw-bold.text-dark.m-0 📄 Chi tiết nhiệm vụ
                    a.btn-close(href="/" aria-label="Close")

                // 2. Tên công việc
                div.mb-4
                    div.label-text Tên công việc
                    h4.fw-bold.text-primary #{task.title}

                // 3. Trạng thái & Quan trọng (Nằm cùng 1 dòng)
                div.d-flex.gap-3.mb-4
                    // Badge Trạng thái
                    div
                        div.label-text Trạng thái
                        if task.status == 'completed'
                            span.badge.bg-success.badge-custom ✅ Đã hoàn thành
                        else
                            span.badge.bg-secondary.badge-custom ⏳ Đang thực hiện
                    
                    // Badge Quan trọng (Chỉ hiện nếu có)
                    if task.is_important
                        div
                            div.label-text Mức độ
                            span.badge.bg-danger.badge-custom 🔥 Quan trọng

                // 4. Nội dung chi tiết (Mô tả)
                div.mb-4
                    div.label-text Chi tiết / Ghi chú
                    div.content-box
                        if task.description
                            p.mb-0.text-dark(style="white-space: pre-line;") #{task.description}
                        else
                            p.mb-0.text-muted.fst-italic (Không có mô tả thêm)

                // 5. Ngày tạo
                div.mb-5
                    div.label-text Ngày tạo
                    div.text-muted 
                        i.bi.bi-clock.me-2
                        | #{task.created_at.toLocaleString('vi-VN')}

                // 6. Các nút hành động
                div.d-grid.gap-2.d-md-flex.justify-content-md-end
                    a.btn.btn-outline-secondary.px-4(href="/") 
                        i.bi.bi-arrow-left.me-2
                        | Quay lại
                    
                    a.btn.btn-primary.px-4(href=`/edit/${task.id}`) 
                        i.bi.bi-pencil.me-2
                        | Chỉnh sửa