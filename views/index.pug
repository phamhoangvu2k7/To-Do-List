doctype html
html
    head
        title To-Do List
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet")
    body.container.mt-5
        h1.text-center.mb-4 Danh sách việc cần làm
    
        // Nút thêm mới (sẽ làm sau)
        div.text-end.mb-3
            a.btn.btn-primary(href="/create") + Thêm việc mới
    
        // Bảng danh sách
        if tasks.length == 0
            div.alert.alert-warning Chưa có công việc nào.
        else
            ul.list-group
                each task in tasks
                    li.list-group-item #{task.title} - #{task.status}